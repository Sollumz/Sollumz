TODO 
==============

-make materials have a global variable for the shader name since it can be changed, add in the name in the 
 shader operators lookup table.... pain 

-figure out a way to change texture name to match the newly selected texture in the ui

-figure out the precision of the floats being represented as doubles in xml 
